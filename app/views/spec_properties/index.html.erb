<h1>Listing spec_properties</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @spec_properties.each do |spec_property| %>
  <tr>
    <td><%= link_to 'Show', spec_property %></td>
    <td><%= link_to 'Edit', edit_spec_property_path(spec_property) %></td>
    <td><%= link_to 'Destroy', spec_property, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Spec property', new_spec_property_path %>
