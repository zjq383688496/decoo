<h1>Listing in_stocks</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @in_stocks.each do |in_stock| %>
  <tr>
    <td><%= link_to 'Show', in_stock %></td>
    <td><%= link_to 'Edit', edit_in_stock_path(in_stock) %></td>
    <td><%= link_to 'Destroy', in_stock, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New In stock', new_in_stock_path %>
