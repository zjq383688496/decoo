<%= form_for(@in_stock_item) do |f| %>
  <% if @in_stock_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@in_stock_item.errors.count, "error") %> prohibited this in_stock_item from being saved:</h2>

      <ul>
      <% @in_stock_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
